#include <iostream>

using namespace std;

int main(){
    freopen("input.txt", "rt", stdin);
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int answer;
    cin >> answer;
    cout << answer;
    return 0;
}
