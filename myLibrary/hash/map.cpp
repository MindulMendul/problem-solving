#include <iostream>
#include <map>
#include <unordered_map>

using namespace std;

int main(){
  freopen("input.txt", "rt", stdin);
  ios::sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);

  unordered_map<int, int> um;
  map<int, int> om;

  return 0;
}
